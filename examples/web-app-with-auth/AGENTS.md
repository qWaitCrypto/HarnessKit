# AGENTS.md

This example repository uses HarnessKit.

## Start Here

1. Read `AGENTS.md`.
2. Read `ARCHITECTURE.md`.
3. Read `docs/index.md`.
4. For auth work, read `docs/product-specs/login.md`, then `docs/design-docs/auth.md`, then `docs/decisions/0001-auth-boundary.md`.

Do not scan every doc by default. Ask HarnessKit for a context packet when a task names a feature or subsystem.
