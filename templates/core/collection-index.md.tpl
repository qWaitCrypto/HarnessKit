# {{collection_title}}

Authoritative manifest for `{{collection_repo_path}}/`.

## Purpose

{{collection_purpose}}

## When To Read

{{collection_when_to_read}}

## Defaults

- Default authority: `{{default_authority}}`
- Default status: `{{default_status}}`
- Anchor candidate: {{anchor_guidance}}
- Preferred template: `{{template_path}}`
- Allowed frontmatter: {{allowed_frontmatter}}

## Current Entry Docs

- None yet. Add the narrowest focused docs here when they become repeated reading entrypoints.

## Create A Missing Doc

1. Start from `{{template_path}}`.
2. Keep the new file narrow and task-scoped.
3. Update this index when the new file becomes a repeated reading entrypoint.

## Reading Path

`AGENTS.md` or `CLAUDE.md` -> `{{architecture_path}}` -> `{{manifest_path}}` -> `{{collection_index_path}}` -> focused doc
